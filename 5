#include <iostream>
using namespace std;

int main()
{
int M;
cin >> M;
int N;
int *m = new int[N];
int *c = new int[N];
double *k = new double[N];
    for(int i = 0; i < N; i++)
{
cout << "Масса: "; cin >> m[i];
cout << "Цена: "; cin >> c[i];
k[i] = m[i] / c[i];
}
    
double max = w[0];
int j = 0;
    for(int i = 1; i < N; i++)
{
    if(max < k[i])
{
max = k[i];
j = i;
}
}

double k = M / m[j];
cout << "Рюкзак можно наполнить вещами стоимостью: " << W*c[j];
return 0;
}
